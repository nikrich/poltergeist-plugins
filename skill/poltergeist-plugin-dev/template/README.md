# My Plugin

One paragraph on what your plugin does — this file is rendered on your
marketplace page at `market.getpoltergeist.com/plugins/<id>/`.

## Install

In Poltergeist: **Plugins → install from git**

- git url: `https://github.com/you/my-plugin`

## Develop

```
npm install
npm run build   # bundles src/ → dist/ — commit dist/, installs never build
```
